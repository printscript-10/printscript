package utils

enum class TokenType {
    WHITESPACE,
    STRING,
    VARIABLE_DECLARATOR,
    TYPE,
    PRINT,
    IDENTIFIER,
    NUMBER,
    COLON,
    SEMICOLON,
    OPEN_BRACKET,
    CLOSE_BRACKET,
    BINARY_OPERATOR,
    ASSIGN,
    IF,
    ELSE,
}
